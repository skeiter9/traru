'use strict';

describe('Service: gmap', () => {

  let gmap = null;

  beforeEach(() => {
    module('gmap');
    inject((_gmap_) => {
      gmap = _gmap_;
    });
  });

  it('check default coordinates', () => {
    //
  });

});
